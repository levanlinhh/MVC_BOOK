
<div class="list-group">
    @foreach (var chude in Model)
    {
        <a href="@Url.Action("Sachtheochude","BookStore", new {id = chude.MaCD})" class="list-group-item"> @chude.TenChuDe</a>

    }
</div>
